create table EMPLOYEES(
                          id bigint not null,
                          name varchar(25),
                          primary key (id)
);

-- Create table Student(
--                         id bigint not null,
--                         firstname varchar(25),
--                         lastname varchar(25),
--                         email varchar(25),
--                         primary key (id)
-- );